# IG-Installer
Instagram Installer 
$ apt update && apt upgrade 
$ apt install git
$ git clone https://github.com/InYourG00D1/IG-Installer
$ gem install lolcat 
$ ls
$ cd IG-Installer
$ sh IG-tool.sh
